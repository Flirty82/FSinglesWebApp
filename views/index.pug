extends layout

block content
  h1= Flirting Singles
  p Welcome to #{Flirting Singles}

  header
   h1 Welcome, to Flirting Singles!
   p Discover and connect with other singles.  Interact with other members via exciting features flirts, invites,
   video profiles, chat rooms, feedback feature, virtual dating and so much more!

   section#posts
    eachPostInPosts
     article.post
     h2 post.title
      p B#{post.author}on post.date
      p=post.content
      if post.comments.length>0
       h3 Comments:
       ul.comments
        each comment in post.comments
         li
           strong=comment.user
           p=comment.text
        else
         p No comments yet.  Be the first to comment!

         extends layouts
          layout
           block content
             header
              h1 Activity Feed
              p Create posts, share photos, videos, and more!
              section#posts
               p LoadingPosts...

               if user.SubscriptionType= 'gold'
               h1 Welcome gold member!
            else
             h1 Upgrade to access more features!

