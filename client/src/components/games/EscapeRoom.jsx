import React, { useState } from 'react';

const EscapeRoom = () => {
    const puzzles = [
        clue, 'You find a locked box with a 3-digit code.  The note says: "The code ist the number of the legs on 3 chairs plus the number of corners on a square.',
        answer, "16"
    ],
}